name: cielo_zero_auth
description: A Flutter package that helps to use Cielo's Zero Auth.
version: 1.0.1
homepage: https://github.com/developercielo/cielo-zero-auth-dart

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  cielo_oauth: ^1.0.2
  json_annotation: ^3.0.0

dev_dependencies:
  build_runner: ^1.0.0
  json_serializable: ^3.3.0
  flutter_test:
    sdk: flutter

flutter: